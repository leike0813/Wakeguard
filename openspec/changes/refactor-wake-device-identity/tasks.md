## 1. Family Identity Model

- [x] 1.1 明确 `stable_id` 的业务语义为治理族群 ID
- [x] 1.2 定义受管 ID 仅允许 `vidpid:*` 与 `sys:*`
- [x] 1.3 明确 `name:*` 仅用于诊断，不参与自动治理

## 2. Scan and Aggregation

- [x] 2.1 在实现上移除“显示名 + 编号 + 候选顺序”的实例级启发式映射
- [x] 2.2 引入设备拓扑快照与 wake 叶子快照，并按父链解析结果归并到族群 ID
- [x] 2.3 定义无法归并到受管族群时的跳过、父链失败日志与告警行为

## 3. Policy and Execution

- [x] 3.1 定义族群级白名单判定规则
- [x] 3.2 定义族群级禁用动作的数据结构与执行语义
- [x] 3.3 明确白名单仅表示“豁免自动禁用”，不自动恢复唤醒

## 4. UI and Logging Semantics

- [x] 4.1 定义 UI 每一行代表一个族群而非实例
- [x] 4.2 定义族群级 `wake_disabled` 的计算语义
- [x] 4.3 定义族群级决策日志、禁用日志、叶子到族群映射日志和跳过日志

## 5. Verification

- [x] 5.1 为父链族群解析、族群归并与白名单语义补充测试场景
- [x] 5.2 为重启后名称编号变化场景补充测试场景
- [x] 5.3 运行 `openspec validate --changes`
